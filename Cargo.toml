[package]
name = "wetware"
version = "0.1.0"
edition = "2024"
description = "Track, organize, and process thoughts"
license = "Apache-2.0"

[[bin]]
name = "wet"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5.1", features = ["derive"] }
